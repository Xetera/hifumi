import { addAction, addMutation, createModule } from "@/mixins/vuex";

export const tags = createModule({
  state: {
    selected: [],
    tags: [],
    tagCount: 0
  },
  mutations: {
  },
  actions: {
  }
});
