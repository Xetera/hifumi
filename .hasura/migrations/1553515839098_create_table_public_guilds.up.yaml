- args:
    sql: CREATE TABLE "public"."guilds"("id" serial NOT NULL, "guild_id" text NOT
      NULL UNIQUE, "enabled" boolean NOT NULL DEFAULT True, "welcome_channel" text
      , PRIMARY KEY ("id","guild_id") );
  type: run_sql
- args:
    name: guilds
    schema: public
  type: add_existing_table_or_view
