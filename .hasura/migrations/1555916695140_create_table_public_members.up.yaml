- args:
    sql: CREATE TABLE "public"."members"("id" serial NOT NULL UNIQUE, "user_id" text
      NOT NULL, "guild_id" text NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("user_id")
      REFERENCES "public"."users"("user_id") ON UPDATE restrict ON DELETE cascade,
      FOREIGN KEY ("guild_id") REFERENCES "public"."guilds"("guild_id") ON UPDATE
      restrict ON DELETE cascade);
  type: run_sql
- args:
    name: members
    schema: public
  type: add_existing_table_or_view
