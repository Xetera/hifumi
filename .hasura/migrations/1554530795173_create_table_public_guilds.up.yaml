- args:
    sql: CREATE TABLE "public"."guilds"("guild_id" text NOT NULL UNIQUE, "enabled"
      boolean NOT NULL, "welcome_channel" text , "name" text NOT NULL, PRIMARY KEY
      ("guild_id") );
  type: run_sql
- args:
    name: guilds
    schema: public
  type: add_existing_table_or_view
