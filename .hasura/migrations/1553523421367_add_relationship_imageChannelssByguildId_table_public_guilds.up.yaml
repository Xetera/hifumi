- args:
    name: imageChannelssByguildId
    table:
      name: guilds
      schema: public
    using:
      foreign_key_constraint_on:
        column: guild_id
        table:
          name: image_channels
          schema: public
  type: create_array_relationship
