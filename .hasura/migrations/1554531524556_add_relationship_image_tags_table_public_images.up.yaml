- args:
    name: image_tags
    table:
      name: images
      schema: public
    using:
      foreign_key_constraint_on:
        column: image_id
        table:
          name: image_tags
          schema: public
  type: create_array_relationship
