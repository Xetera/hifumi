- args:
    name: image_tags
    table:
      name: users
      schema: public
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: image_tags
          schema: public
  type: create_array_relationship
