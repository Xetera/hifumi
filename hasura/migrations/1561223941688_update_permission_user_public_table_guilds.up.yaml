- args:
    role: user
    table:
      name: guilds
      schema: public
  type: drop_select_permission
- args:
    permission:
      allow_aggregations: true
      columns:
      - banner
      - guild_id
      - icon
      - name
      filter:
        members:
          user_id:
            _eq: X-Hasura-User-Id
      limit: 100
    role: user
    table:
      name: guilds
      schema: public
  type: create_select_permission
