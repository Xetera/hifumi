- args:
    sql: CREATE TABLE "public"."users"("id" serial NOT NULL UNIQUE, "user_id" text
      NOT NULL UNIQUE, "name" text NOT NULL, "avatar" text , PRIMARY KEY ("id","user_id")
      );
  type: run_sql
- args:
    name: users
    schema: public
  type: add_existing_table_or_view
