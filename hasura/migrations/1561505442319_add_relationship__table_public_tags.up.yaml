- args:
    name: image_tags
    table:
      name: tags
      schema: public
    using:
      manual_configuration:
        column_mapping:
          guild_id: guild_id
          name: name
        remote_table:
          name: image_tags
          schema: public
  type: create_array_relationship
