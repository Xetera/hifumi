- args:
    sql: CREATE TABLE "public"."stars"("member_id" text NOT NULL, "image_id" integer
      NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "guild_id" text NOT
      NULL, PRIMARY KEY ("member_id","image_id") , FOREIGN KEY ("member_id", "guild_id")
      REFERENCES "public"."members"("user_id", "guild_id") ON UPDATE cascade ON DELETE
      cascade, FOREIGN KEY ("image_id") REFERENCES "public"."images"("id") ON UPDATE
      cascade ON DELETE cascade);
  type: run_sql
- args:
    name: stars
    schema: public
  type: add_existing_table_or_view
