subscription ImagesSubscription($where: images_bool_exp, $limit: Int = 40) {
  images(where: $where, order_by: { created_at: desc_nulls_last }, limit: $limit) {
    url
    image_tags {
      name
    }
  }
}
